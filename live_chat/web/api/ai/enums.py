from enum import Enum


class DurationSummarizationEnum(Enum):
    """Enum for duration summarization."""

    DAY = "day"
    THREE_DAYS = "three_days"
    WEEK = "week"
    TWO_WEEKS = "two_weeks"
    MONTH = "month"
